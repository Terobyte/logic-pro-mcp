# P0 спайки — результаты (2026-09)

Logic Pro: <версия из `LogicApp.version()`> · macOS 15 · Logic на отдельном Space, пользователь в Terminal.
Шаблон раздела: Вопрос → Команды → Наблюдения → Ответ (🟢/🟡/🔴) → Решение для спека → Якоря.

## Сводка
| Спайк | Ответ | Решение |
|---|---|---|

## S12 Полнота и цена AX
## S3 Регионы и take folder
## S6 Шаги и точность параметров
## S8 Невидимый select и возврат
## S9 Меню-бар и заголовок undo
## S1 Окна и фокус
## S11 Транспорт через control bar и MMC
## S2 AXObserver
## S4 Импорт/экспорт MIDI
## S5 Вложенные popup
## S7 Слот Search and Add
## S10 Источник аудио

## Ground truth «бело красного» (по UI Logic, глазами)
| № | Имя в UI | Тип | Выход есть? | Дублей |
|---|---|---|---|---|

## Якоря (вход для LocaleTable.en)
| Anchor | Строка | Где (роль/атрибут) | Спайк |
|---|---|---|---|

## Приёмка P1

- **§12.1 readback:** в коде `RecipeRunner` мутации не синтезируют `ok` без readback (офлайн `RecipeRunnerTests`). Живой readback не прогонялся. **BLOCKED.**
- **§12.5 CompletenessTests:** self-check — `CompletenessTests` PASS при пустом ledger (`entries: []`); capability не рекламируется, полноту ledger не заявляю. Live-проверка ledger **BLOCKED.**
- **§12.6 офлайн LogicKitTests / LogicMCPTests:** self-check — LogicKitTests: 75 executed, 3 skipped, 0 failures; LogicMCPTests: 4 executed, 0 failures. **BLOCKED** (live).
- **§12.11 Live.require / fixture-\*:** гард в `Tests/LogicLiveTests/Live.swift` + `FixtureGuard`; live-suite **не** выполнялась (`LOGIC_LIVE != 1`). **BLOCKED.**
- **§12.12–13 LiveBudgetTests / LiveGuardTests:** файлы на диске; против Logic **не** запускались — нужны `Tests/Fixtures/projects/fixture-*.logicx` и `Scripts/fixture-open.sh`. **BLOCKED.**
- **Sources grep** (`ChannelRouter`, `StatePoller`, `CGEventChannel`, `AppleScriptChannel`): self-check — совпадений в `Sources/` нет (grep exit 1).
